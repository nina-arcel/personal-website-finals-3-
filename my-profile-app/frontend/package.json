{
  "name": "frontend",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "preinstall": "echo 'Node version: ' && node --version && echo 'NPM version: ' && npm --version",
    "dev": "vite",
    "build": "npx vite build",
    "preview": "vite preview",
    "postinstall": "ls -la node_modules/.bin || echo 'No .bin folder'",
    "vercel-build": "npm install && npx vite build"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "@vitejs/plugin-react": "^4.0.0",
    "vite": "^5.4.21"
  }
}